RewriteEngine On
Options -Indexes

# Redirigir errores
ErrorDocument 404 /nutriexperto/index.php

# Proteger carpeta includes
<FilesMatch "\.(php)$">
    <If "%{REQUEST_URI} =~ m|/includes/|">
        Require all denied
    </If>
</FilesMatch>